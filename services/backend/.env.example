# Copy this file to .env and fill in your values.
# The backend works WITHOUT an API key (falls back to mock AI responses).

# ── Demo Mode ────────────────────────────────────────────────────────────────
# DEMO_MODE=true  → guaranteed stable; auto-fallback to mock on any error
# DEMO_MODE=false → production mode
DEMO_MODE=true

# AI_MODE=mock    → keyword rules only, never calls Claude API
# AI_MODE=real    → try Claude first, fallback to mock on any error
AI_MODE=mock

# ── Claude API (required for AI_MODE=real) ───────────────────────────────────
ANTHROPIC_API_KEY=
CLAUDE_MODEL=claude-sonnet-4-6

# ── Server ───────────────────────────────────────────────────────────────────
HOST=0.0.0.0
PORT=8000

# ── CORS: comma-separated origins (use * for open demo) ─────────────────────
CORS_ORIGINS=*

# ── SQLite database ───────────────────────────────────────────────────────────
DB_PATH=./otter_records.db
